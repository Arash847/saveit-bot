# ── Flet (UI framework) ──────────────────────────────────────
flet==0.80.5

# ── Telethon (Telegram client) ───────────────────────────────
# Its deps (pyaes, rsa, pyasn1) are installed from source via
# --source-packages flag in the GitHub Actions workflow.
telethon>=1.36.0,<2.0

